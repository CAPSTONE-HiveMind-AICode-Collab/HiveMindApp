import structuredOutputCore from "@/lib/ai/structuredOutputCore.cjs";

export const {
  cleanString,
  cleanStringArray,
  extractJsonObject,
  normalizeChatCitationPayload,
  normalizeDecisionRecordPayload,
  normalizeBriefingPayload,
} = structuredOutputCore;
